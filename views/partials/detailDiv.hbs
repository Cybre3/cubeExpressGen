<h1>{{cube.name}}</h1>
<img class="cube" src="{{cube.image_URL}}" />
<div class="details">
  <p><span>Description:</span>
    {{cube.description}}
  </p>
  <p><span>Difficulty level:</span>{{cube.level}}</p>
  <a class="btn" href="/">Back</a>
  {{#if token}}
  <a class="btn" href="/edit/{{cube._id}}">Edit</a>
  <a class="btn" href="/delete/{{cube._id}}">Delete</a>
  <a class="btn" href="/attach/accessory/{{cube._id}}">Attach Accessory</a>
  {{/if}}
</div>

{{! Magnets in AirSM will not drop, and their positions will be more precise
    with the Magnets-Snap-On design. With the use of 3mm*2mm magnets, the
    handfeel will be more stable and more comfortable. P.S. This design is brand
    new for the AirSM. }}